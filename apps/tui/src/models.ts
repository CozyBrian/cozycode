// Model discovery now lives in @cozycode/core so every frontend shares it.
// Re-exported here to keep existing TUI imports working unchanged.
export { fetchModels, mergeModels } from "@cozycode/core";
